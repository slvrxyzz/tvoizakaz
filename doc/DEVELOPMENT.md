# 🛠️ Разработка TeenFreelance

## 🚀 Быстрый старт для разработчиков

### Установка
```bash
# Клонирование репозитория
git clone <repository-url>
cd backend-main

# Установка зависимостей
pip install -e ".[dev,test]"

# Запуск приложения
cd src
python main.py
```

### Docker разработка
```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
curl http://localhost:8000/health
```

## 📦 Управление зависимостями

### Установка групп зависимостей
```bash
# Только основные зависимости
pip install -e .

# С dev зависимостями (форматирование, линтинг)
pip install -e ".[dev]"

# С test зависимостями (тестирование)
pip install -e ".[test]"

# С docs зависимостями (документация)
pip install -e ".[docs]"

# Все зависимости
pip install -e ".[all]"
```

### Добавление новых зависимостей
1. Отредактируйте `pyproject.toml`
2. Добавьте зависимость в соответствующую группу
3. Установите: `pip install -e ".[group]"`
4. Закоммитьте изменения

## 🔧 Инструменты разработки

### Форматирование кода
```bash
# Black (автоматическое форматирование)
black src/

# isort (сортировка импортов)
isort src/

# Оба сразу
black src/ && isort src/
```

### Линтинг
```bash
# Ruff (быстрый линтер)
ruff check src/

# Flake8 (классический линтер)
flake8 src/

# Исправление автоматически исправимых ошибок
ruff check --fix src/
```

### Проверка типов
```bash
# MyPy (проверка типов)
mypy src/

# Проверка конкретного файла
mypy src/main.py
```

### Тестирование
```bash
# Запуск всех тестов
pytest

# С покрытием кода
pytest --cov=src

# Конкретный тест
pytest tests/test_auth.py

# С verbose выводом
pytest -v

# Только быстрые тесты
pytest -m "not slow"
```

## 🏗️ Архитектура проекта

### Структура src/
```
src/
├── main.py                 # Точка входа
├── config.py              # Конфигурация
├── domain/                 # Доменный слой
│   ├── entities/           # Сущности
│   ├── interfaces/         # Интерфейсы
│   └── services/           # Бизнес-логика
├── infrastructure/         # Инфраструктурный слой
│   ├── cache/              # Кэширование
│   ├── di/                 # DI контейнер
│   ├── monitoring/         # Мониторинг
│   ├── repositories/       # Репозитории
│   ├── security/           # Безопасность
│   └── services/           # Инфраструктурные сервисы
└── presentation/           # Слой представления
    └── api/v1/             # API endpoints
```

### Принципы разработки
1. **Clean Architecture** - Четкое разделение слоев
2. **Dependency Injection** - Управление зависимостями
3. **Type Hints** - Типизация всех функций
4. **Async/Await** - Асинхронное программирование
5. **Error Handling** - Обработка ошибок

## 🧪 Тестирование

### Типы тестов
- **Unit тесты** - Тестирование отдельных функций
- **Integration тесты** - Тестирование интеграции компонентов
- **API тесты** - Тестирование API endpoints

### Запуск тестов
```bash
# Все тесты
pytest

# Только unit тесты
pytest -m unit

# Только integration тесты
pytest -m integration

# С покрытием
pytest --cov=src --cov-report=html
```

### Написание тестов
```python
import pytest
from fastapi.testclient import TestClient
from src.main import app

client = TestClient(app)

def test_health_check():
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json()["status"] == "healthy"
```

## 📚 Документация

### Генерация документации
```bash
# Установка docs зависимостей
pip install -e ".[docs]"

# Запуск локального сервера
mkdocs serve

# Сборка документации
mkdocs build
```

### API документация
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 🔍 Отладка

### Логирование
```python
from src.infrastructure.monitoring.logger import logger

logger.info("User created", user_id=123)
logger.error("Database error", error=str(e))
```

### Переменные окружения
```bash
# Создание .env файла
cp .env.example .env

# Редактирование конфигурации
nano .env
```

### Docker отладка
```bash
# Логи контейнера
docker-compose logs backend

# Вход в контейнер
docker-compose exec backend bash

# Перезапуск сервиса
docker-compose restart backend
```

## 🚀 Деплой

### Production сборка
```bash
# Сборка Docker образа
docker build -t teenfreelance:latest .

# Запуск в production
docker-compose -f docker-compose.prod.yml up -d
```

### Мониторинг
- **Health Check**: `/health`
- **Метрики**: Встроенные в FastAPI
- **Логи**: Структурированное JSON логирование

## 🤝 Git Workflow

### Ветки
- `main` - Production код
- `develop` - Development код
- `feature/*` - Новые функции
- `hotfix/*` - Критические исправления

### Коммиты
```bash
# Форматирование перед коммитом
black src/ && isort src/

# Коммит
git add .
git commit -m "feat: add user authentication"

# Push
git push origin feature/user-auth
```

### Pre-commit хуки
```bash
# Установка pre-commit
pip install pre-commit

# Установка хуков
pre-commit install

# Запуск на всех файлах
pre-commit run --all-files
```

## 📋 Чеклист разработки

### Перед коммитом
- [ ] Код отформатирован (black, isort)
- [ ] Линтинг пройден (ruff, flake8)
- [ ] Типы проверены (mypy)
- [ ] Тесты написаны и проходят
- [ ] Документация обновлена

### Перед PR
- [ ] Все тесты проходят
- [ ] Покрытие кода > 80%
- [ ] Документация обновлена
- [ ] Changelog обновлен
- [ ] Code review пройден

---

**Удачной разработки! 🚀**







