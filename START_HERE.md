# 🚀 TeenFreelance - Инструкция по запуску

## ✅ Что было сделано для интеграции фронтенда и бэкенда:

1. ✅ Настроен CORS для работы фронтенда на порту 3000
2. ✅ Созданы скрипты запуска для Windows и Linux/Mac
3. ✅ Настроена передача токенов для WebSocket
4. ✅ Созданы примеры конфигурационных файлов

## 🎯 Быстрый старт (3 шага)

### Шаг 1: Настройка бэкенда

**Создайте файл `.env` в корне проекта:**

```env
SECRET_KEY=ваш-секретный-ключ-минимум-32-символа-длинный-для-безопасности
DATABASE_URL=sqlite+aiosqlite:///./teenfreelance.db
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
DEBUG=true
ENVIRONMENT=development
```

**Запустите бэкенд:**

Windows:
```bash
start_backend.bat
```

Linux/Mac:
```bash
chmod +x start_backend.sh
./start_backend.sh
```

Бэкенд будет на: **http://localhost:8000** 📡

### Шаг 2: Настройка фронтенда

**Перейдите в директорию фронтенда:**
```bash
cd fronted
```

**Создайте файл `.env.local`:**
```env
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
NEXT_PUBLIC_MODE=dev
```

**Запустите фронтенд:**

Windows:
```bash
start_frontend.bat
```

Linux/Mac:
```bash
chmod +x start_frontend.sh
./start_frontend.sh
```

Фронтенд будет на: **http://localhost:3000** 🌐

### Шаг 3: Откройте сайт!

Откройте браузер: **http://localhost:3000**

## 📋 Что работает:

- ✅ Регистрация и авторизация
- ✅ Создание и просмотр заказов
- ✅ Чаты в реальном времени (WebSocket)
- ✅ Профили пользователей
- ✅ Портфолио
- ✅ Избранное
- ✅ Рейтинги и отзывы

## 🔧 Полезные ссылки:

- API документация: http://localhost:8000/docs
- Health check: http://localhost:8000/health
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## ⚠️ Важно:

1. **Сначала запустите бэкенд**, потом фронтенд
2. Порты 3000 и 8000 должны быть свободны
3. Для продакшена настройте MySQL в `.env` вместо SQLite

## 🐛 Решение проблем:

### "Cannot connect to API"
- Убедитесь, что бэкенд запущен: http://localhost:8000/health
- Проверьте `NEXT_PUBLIC_API_URL` в `.env.local`

### CORS ошибки
- Проверьте `CORS_ORIGINS` в `.env` бэкенда
- Убедитесь, что адрес фронтенда в списке

### WebSocket не работает
- Проверьте, что токен сохраняется при логине
- Откройте консоль браузера для отладки

## 📚 Дополнительная документация:

- `SETUP.md` - Подробная инструкция по настройке
- `QUICK_START.md` - Быстрый старт
- `doc/` - Техническая документация

---

**Готово! Ваш сайт должен работать! 🎉**

Если что-то не работает, проверьте консоль браузера и логи бэкенда.


