# Flow Tests для Freelance Platform

Полноценные flow тесты для API эндпоинтов, WebSocket соединений и производительности.

## 📁 Структура тестов

```
tests/
├── conftest.py                 # Конфигурация pytest и фикстуры
├── pytest.ini                 # Настройки pytest
├── run_flow_tests.py          # Скрипт запуска тестов
├── README.md                  # Документация тестов
├── requirements.txt           # Зависимости для тестов
├── logs/                      # Логи тестов
├── test_api_flows.py          # Flow тесты API эндпоинтов
├── test_websocket_flows.py    # Flow тесты WebSocket
├── test_performance_flows.py  # Тесты производительности
├── test_simple_api.py         # Простые API тесты
└── test_business_logic.py     # Тесты бизнес-логики
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка основных зависимостей
pip install pytest httpx pytest-asyncio

# Установка дополнительных зависимостей для производительности
pip install psutil pytest-xdist pytest-benchmark

# Установка зависимостей для покрытия кода
pip install pytest-cov
```

### 2. Запуск тестов

#### Быстрый запуск (рекомендуется)

```bash
# Linux/Mac
chmod +x tests/run_all_flow_tests.sh
./tests/run_all_flow_tests.sh all

# Windows
tests\run_all_flow_tests.bat all
```

#### Детальный запуск

```bash
# Запуск всех flow тестов
python tests/run_flow_tests.py all

# Запуск только API flow тестов
python tests/run_flow_tests.py flow

# Запуск WebSocket тестов
python tests/run_flow_tests.py websocket

# Запуск тестов производительности
python tests/run_flow_tests.py performance

# Запуск тестов безопасности
python tests/run_flow_tests.py security

# Запуск тестов интеграции
python tests/run_flow_tests.py integration

# Запуск стресс-тестов
python tests/run_flow_tests.py stress

# Запуск с покрытием кода
python tests/run_flow_tests.py coverage
```

### 3. Прямой запуск через pytest

```bash
# Все тесты
pytest tests/ -v

# Конкретный файл
pytest tests/test_api_flows.py -v

# Конкретный тест
pytest tests/test_api_flows.py::TestAPIFlows::test_user_registration_and_login_flow -v

# Тесты с маркерами
pytest tests/ -m "not slow" -v
pytest tests/ -m "performance" -v
```

## 📋 Категории тестов

### 🔄 API Flow Tests (`test_api_flows.py`)

Полные пользовательские сценарии через HTTP API:

- **Регистрация и аутентификация** - полный flow регистрации, входа и получения профиля
- **Управление заказами** - создание, просмотр, обновление заказов
- **Система заявок** - подача заявок исполнителями на заказы
- **Чаты и сообщения** - создание чатов и обмен сообщениями
- **Управление профилем** - обновление данных пользователя
- **Поиск и фильтрация** - поиск заказов и пользователей
- **Избранное** - управление избранными заказами
- **Рейтинги и отзывы** - система оценок и отзывов
- **Обработка ошибок** - валидация данных и обработка ошибок
- **CORS и безопасность** - проверка CORS и заголовков безопасности

### 🌐 WebSocket Flow Tests (`test_websocket_flows.py`)

Тестирование реального времени общения:

- **Подключение к WebSocket** - установка соединения
- **Аутентификация через WebSocket** - авторизация в WebSocket
- **Чат через WebSocket** - обмен сообщениями в реальном времени
- **Обработка ошибок** - обработка ошибок в WebSocket
- **Heartbeat** - проверка связи
- **Множественные подключения** - несколько соединений одновременно
- **Типы сообщений** - текстовые, файловые, системные сообщения
- **Управление комнатами** - создание и управление чат-комнатами
- **Уведомления** - система уведомлений через WebSocket
- **Восстановление соединения** - переподключение при разрыве

### ⚡ Performance Tests (`test_performance_flows.py`)

Тестирование производительности и нагрузочное тестирование:

- **Время ответа** - измерение времени ответа API
- **Конкурентные запросы** - множественные одновременные запросы
- **Использование памяти** - мониторинг потребления памяти
- **Производительность БД** - скорость операций с базой данных
- **Пропускная способность** - количество запросов в секунду
- **Обработка ошибок** - производительность обработки ошибок
- **Конкурентная регистрация** - множественная регистрация пользователей
- **Большие payload'ы** - обработка больших данных
- **WebSocket производительность** - скорость WebSocket сообщений
- **Кэш производительность** - эффективность кэширования
- **Пул соединений БД** - тестирование пула соединений
- **Обнаружение утечек памяти** - поиск утечек памяти
- **Стресс-тестирование** - длительная нагрузка на API

## 🏷️ Маркеры тестов

```python
@pytest.mark.slow          # Медленные тесты (>5 сек)
@pytest.mark.integration   # Интеграционные тесты
@pytest.mark.unit         # Модульные тесты
@pytest.mark.api          # Тесты API
@pytest.mark.websocket    # Тесты WebSocket
@pytest.mark.performance  # Тесты производительности
@pytest.mark.flow         # Flow тесты
@pytest.mark.auth         # Тесты аутентификации
@pytest.mark.database     # Тесты базы данных
@pytest.mark.cache        # Тесты кэша
@pytest.mark.error        # Тесты обработки ошибок
@pytest.mark.concurrent   # Тесты конкурентности
@pytest.mark.memory       # Тесты памяти
@pytest.mark.stress       # Стресс-тесты
```

## 🔧 Конфигурация

### pytest.ini

Основные настройки pytest:

```ini
[tool:pytest]
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short --strict-markers --disable-warnings --color=yes --durations=10 -v
asyncio_mode = auto
timeout = 300
```

### Фикстуры

В `conftest.py` определены основные фикстуры:

- `app` - FastAPI приложение
- `client` - HTTP тестовый клиент
- `async_client` - асинхронный HTTP клиент
- `db_session` - сессия тестовой БД
- `test_user_data` - тестовые данные пользователя
- `auth_headers` - заголовки авторизации
- `test_user` - создание тестового пользователя
- `test_category` - создание тестовой категории
- `test_order` - создание тестового заказа

## 📊 Отчеты и логи

### Логи тестов

Логи сохраняются в `tests/logs/`:
- `pytest.log` - общие логи pytest
- Отдельные логи для каждого типа тестов

### Покрытие кода

```bash
# Генерация отчета покрытия
python tests/run_flow_tests.py coverage

# HTML отчет будет в htmlcov/
```

### Бенчмарки

```bash
# Установка pytest-benchmark
pip install pytest-benchmark

# Запуск бенчмарков
pytest tests/test_performance_flows.py --benchmark-only
```

## 🐛 Отладка тестов

### Подробный вывод

```bash
# Максимально подробный вывод
pytest tests/ -vvv --tb=long

# Остановка на первой ошибке
pytest tests/ -x

# Запуск только упавших тестов
pytest tests/ --lf
```

### Отладка конкретного теста

```bash
# Запуск с отладчиком
pytest tests/test_api_flows.py::TestAPIFlows::test_user_registration_and_login_flow -v -s

# Запуск с логированием
pytest tests/ --log-cli-level=DEBUG
```

### Изоляция тестов

```bash
# Запуск тестов в изоляции
pytest tests/ --forked

# Запуск без кэша
pytest tests/ --cache-clear
```

## 🔄 CI/CD интеграция

### GitHub Actions

```yaml
name: Flow Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest httpx pytest-asyncio
      - name: Run flow tests
        run: python tests/run_flow_tests.py all
```

### Docker

```bash
# Запуск тестов в Docker
docker-compose exec backend python tests/run_flow_tests.py all

# Запуск тестов в отдельном контейнере
docker run --rm -v $(pwd):/app -w /app python:3.9 python tests/run_flow_tests.py all
```

## 📈 Мониторинг производительности

### Метрики

Тесты измеряют:
- Время ответа API
- Пропускную способность (RPS)
- Использование памяти
- Время выполнения операций БД
- Эффективность кэша

### Алерты

Настройте алерты для:
- Время ответа > 1 секунды
- Ошибки > 5%
- Использование памяти > 100MB
- Пропускная способность < 10 RPS

## 🛠️ Расширение тестов

### Добавление нового flow теста

1. Создайте новый тест в соответствующем файле
2. Используйте существующие фикстуры
3. Добавьте маркеры для категоризации
4. Обновите документацию

### Добавление новой категории тестов

1. Создайте новый файл `test_*.py`
2. Добавьте маркер в `pytest.ini`
3. Обновите `run_flow_tests.py`
4. Добавьте документацию

## 📚 Дополнительные ресурсы

- [pytest документация](https://docs.pytest.org/)
- [FastAPI тестирование](https://fastapi.tiangolo.com/tutorial/testing/)
- [WebSocket тестирование](https://fastapi.tiangolo.com/advanced/websockets/)
- [Производительность Python](https://docs.python.org/3/library/profile.html)
