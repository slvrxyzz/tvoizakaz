# Быстрый запуск 

## Шаг 1: Настройка бэкенда

1. **Создайте файл `.env` в корне проекта:**
```bash
# Скопируйте пример
cp .env.example .env
```

2. **Отредактируйте `.env` и укажите:**
```env
SECRET_KEY=ваш-секретный-ключ-минимум-32-символа-длинный
DATABASE_URL=sqlite+aiosqlite:///./teenfreelance.db
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000
DEBUG=true
ENVIRONMENT=development
```

3. **Запустите бэкенд:**

**Windows:**
```bash
start_backend.bat
```

**Linux/Mac:**
```bash
chmod +x start_backend.sh
./start_backend.sh
```

Бэкенд будет доступен на: **http://localhost:8000**
- API документация: http://localhost:8000/docs
- Health check: http://localhost:8000/health

## Шаг 2: Настройка фронтенда

1. **Перейдите в директорию фронтенда:**
```bash
cd fronted
```

2. **Создайте файл `.env.local`:**
```bash
# Если есть пример
cp env.local.example .env.local
# или (если файл с точкой доступен в вашей системе)
cp .env.example .env.local
```

Или создайте вручную с содержимым:
```env
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
NEXT_PUBLIC_MODE=dev
```

3. **Установите зависимости и запустите:**
```bash
npm install
npm run dev
```

**Или используйте скрипт:**

**Windows:**
```bash
start_frontend.bat
```

**Linux/Mac:**
```bash
chmod +x start_frontend.sh
./start_frontend.sh
```

Фронтенд будет доступен на: **http://localhost:3000**

## Шаг 3: Проверка работы

1. Откройте браузер: **http://localhost:3000**
2. Попробуйте зарегистрироваться или войти
3. Проверьте API: **http://localhost:8000/docs**

## Важно!

- Убедитесь, что бэкенд запущен **до** запуска фронтенда
- Порты 3000 (фронтенд) и 8000 (бэкенд) должны быть свободны
- Если возникают CORS ошибки, проверьте `CORS_ORIGINS` в `.env` бэкенда

## Решение проблем

### "Cannot connect to API"
- Проверьте, что бэкенд запущен на порту 8000
- Проверьте `NEXT_PUBLIC_API_URL` в `.env.local`

### CORS ошибки
- Убедитесь, что в `.env` бэкенда указан правильный `CORS_ORIGINS`
- Добавьте ваш адрес фронтенда в список

### База данных
- Для разработки можно использовать SQLite (по умолчанию)
- Для продакшена настройте MySQL/MariaDB в `.env`

## Готово! 🎉

Теперь ваш сайт должен работать. Откройте http://localhost:3000 и начните использовать платформу!
